/**
 * Mock for `server-only` package used by Next.js.
 * In Vitest (server-side tests), this is a no-op.
 * Next.js build replaces this with a module that throws in Client Components.
 */
export {};
